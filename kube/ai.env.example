# AI service environment — copy to /opt/research-ai/ai.env and fill in
LLAMA_BIN=/path/to/llama.cpp/build/bin
MODEL_DIR=/path/to/models
LD_LIBRARY_PATH=/path/to/llama.cpp/build/bin
N_GPU_LAYERS=99

# Per-server auth (AI server issues its own JWTs)
SERVER_AUTH_USER=server-ai
SERVER_AUTH_PASS=your-ai-server-password
JWT_SECRET=generate-a-random-secret-here
